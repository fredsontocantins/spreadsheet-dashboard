#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/html-select@2.3.24/node_modules/html-select/node_modules:/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/html-select@2.3.24/node_modules:/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/html-select@2.3.24/node_modules/html-select/node_modules:/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/html-select@2.3.24/node_modules:/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../bin/cmd.js" "$@"
else
  exec node  "$basedir/../../bin/cmd.js" "$@"
fi
# cmd-shim-target=/tmp/spreadsheet-dashboard/data-dashboard/node_modules/.pnpm/html-select@2.3.24/node_modules/html-select/bin/cmd.js
